import React from 'react';
import ReactDOM from 'react-dom';

const jsx = (
   <div>
    Test if it works
    <p>
        Checking if --watch works fine
    </p>
   </div>
);

ReactDOM.render(jsx, document.getElementById('app'));
